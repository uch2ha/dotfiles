# Windows Terminal Settings

This folder contains my configuration files for Windows Terminal.

- `settings.json` — Main configuration file with default UI, color schemes, and other settings for Windows Terminal.
- PNG files — Custom icons for Ubuntu profiles in Windows Terminal.
